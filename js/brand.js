/* Brand name — change it in exactly one place.
   The string {{BRAND}} is left in each screen's <title> and nav logo, and is
   swapped in at load time for the name set here (js/boot.js does the swap).
   To rename the app, edit only the line below. */
(function () {
  "use strict";
  window.BRAND = "Terra Prep";   /* working name, pending final branding sign-off */
})();
